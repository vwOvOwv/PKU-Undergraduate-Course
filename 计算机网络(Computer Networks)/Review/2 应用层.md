# 应用层
## 1.  C-S和P2P模式
### 模式与协议
-  许多APP有相同的通信模式（C-S），应用层以协议（HTTP...）的形式封装这些共同模式
### Client-Server (C-S)
#### 概念
-  两个应用进程（IP:port）
-  客户：服务请求方（主动请求服务，被服务）
-  服务器：服务提供方（被动接受服务请求，（主动）提供服务）

<div style="text-align: center;">
    <img src="assets/IMG_27.png" width="500" />
</div>

#### 分类
基于面向连接/无连接以及循环服务/并发服务
-  无连接+循环
<div style="text-align: center;">
    <img src="assets/IMG_28.png" width="700" />
</div>

-  无连接+并发：不可行。无连接，服务器无法使用服务套接字与多个客户端通信
    -  如果使用新的服务套接字，客户端不可知新的套接字端口号
    -  可能需要额外的OS支持才能实现并发

-  面向连接+循环

<div style="text-align: center;">
    <img src="assets/IMG_30.png" width="700" />
</div>

-  面向连接+并发
<div style="text-align: center;">
    <img src="assets/IMG_29.png" width="700" />
</div>

### Peer to Peer (P2P)
#### 概念
-  两个进程，不区分服务的请求方和服务的提供方
-  平等对等的通信
-  通信的进程即是客户端也是服务器
#### 实体的特点
可以假设P2P中任意时刻均有大量APP在运行
-  无需总是保持在线
-  可以随时进入和退出
-  任意两个实体间可之间通信
-  易扩展（计算资源共用）

## 2.  Web基本概念
### 万维网（World Wide Web, WWW）
-  IP网络之上的网络，Overlay网络
-  由Web对象，C/S，以及HTTP协议组成
### Web对象
-  Web页面：包含多种对象或链接（指向其他对象的URL）
-  网页，多媒体资源，脚本...
-  分为静态和动态对象，对应静态服务和动态服务
    -  静态对象
        -  文本，表格，图片，图像和视频等多媒体类型的信息（实现语言：标记语言，如HTML，XML，PHP等）
        -  字体、颜色和布局等风格类型的信息（实现语言：层叠样式表CSS）
    -  动态对象
        -  交互信息，比如，用户注册信息、登录信息等（实现：PHP/JSP等语言+MySQL等数据库）
        -  动态页面的创建也可以在浏览器进行
        -  通用网关接口CGI：定义在服务器创建动态文档的过程
        -  脚本语言+数据库：解释程序（可运行在浏览器中）解释执行脚本
-  通过统一资源定位符（URL）定位：**协议类型://主机名:端口//路径和文件名**

## 3.  HTTP🌟
### 基本概念
-  基于TCP，缺省使用HTTP的80端口
-  无状态协议：不记录历史状态信息

### 各版本HTTP与服务过程
#### HTTP/1.0
-  无状态，非持久连接
![Image](assets/IMG_31.png)
-  每次传输都需要经历TCP慢启动阶段
-  收到一个请求的响应后才可发送下一个请求
#### HTTP/1.1
-  支持持久连接+流水线机制
-  缓存策略优化，部分资源请求，断点续传
![Image](assets/IMG_32.png)
-  较少的慢启动
-  Pipeline要求服务器按序响应

#### HTTPS
-  HTTP + SSL/TLS，TCP之上的安全机制

#### HTTP/2.0
-  目标：进一步提高带宽利用率，降低延迟
-  二进制格式、TCP多路复用、头压缩、服务端推送...
    -  允许请求/响应交错（乱序），可自定义报文优先级
    -  主动推送消息：可以用于确认客户端是否在线和预测资源请求，从而支持在应用层上进行流量控制

#### HTTP/3.0
-  QUIC+UDP替代下层TCP
### 报文格式
#### 请求报文
-  开始行（请求行）+首部行+实体主体

<div style="text-align: center;">
    <img src="assets/IMG_33.png" width="500" />
</div>

-  方法（命令）：知名对所请求的对象进行的具体请求操作
    -  GET：请求读取由 URL所标识的对象
    -  POST：向URL标识的对象提交数据（参数、注释...）

![Image](assets/IMG_34.png)

#### 响应报文
-  开始行（状态行）+首部行+实体主体

<div style="text-align: center;">
    <img src="assets/IMG_35.png" width="500" />
</div>

-  状态码
    -  200 OK：请求成功，对象放在实体主体
    -  301 Moved Permanently：对象位置改变，新的位置在响应中通过Location:给出
    -  400 Bad Request：服务器无法解析请求报文
    -  404 Not Found：服务器找不到对应文档
    -  505 HTTP Version Not Supported：服务器不支持对应HTTP版本

### 缓存
#### 基本概念
-  浏览器缓存
    -  浏览器主机上保留访问过的Web页面副本
    -  需要保证副本与服务器中的一致
![Image](assets/IMG_36.png)


-  代理缓存
    -  ISP安装代理缓存服务器，保存客户访问过的Web页面副本
    -  副本供所有用户访问，降低时延，减少冗余流量
<div style="text-align: center;">
    <img src="assets/IMG_37.png" width="300" />
</div>

#### 代理缓存性能分析例子
![Image](assets/IMG_38.png)

![Image](assets/IMG_39.png)

#### 副本一致性问题
-  启发式策略（较少使用）：原始服务器在响应报文中设置Last-Modified头和Expires头
-  询问式策略（常用）：代理服务器询问原始服务器

<div style="text-align: center;">
    <img src="assets/IMG_40.png" width="300" />
</div>

-  注：服务器可以禁止缓存特定页面（no-cache）；需要授权访问的页不应被缓存

### Cookie
#### 基本概念
-  HTTP无状态协议，服务器无法直接得知用户状态，使用cookies维护用户状态
    -  响应报文首部行设置set-cookie:，选择唯一cookie
    -  cookie文件保存在用户主机中，包含服务器返回的一些附加信息，由浏览器管理
    -  服务器建立后端数据库，记录用户信息，cookie作为关键字

<div style="text-align: center;">
    <img src="assets/IMG_41.png" width="300" />
</div>

![Image](assets/IMG_42.png)

#### 内容
-  域：Cookie来源
-  路径：服务器文件系统中哪些部分可以使用该cookie
-  内容：name=value对，最大4KB，是字符串而非可执行程序
-  安全：指示浏览器是否只向使用安全传输连接（HTTPS）的服务器返回cookie

![Image](assets/IMG_43.png)

#### 隐私问题
-  好处：可分析用户喜好，实现个性化推荐
-  坏处：跟踪用户网络浏览痕迹，泄漏用户隐私（可限制浏览器使用cookie）
    -  获取cookie后即可伪造用户请求
-  误区：cookie容易被嵌入木马/间谍程序
    -  cookie中保存的是字符串

## 4.  DNS
### 简介


